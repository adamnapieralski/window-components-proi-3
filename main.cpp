#include <iostream>
#include "wincomponents.h"
#include <deque>

using namespace std;

int main() {
    auto mainComponent = new SignComp('.', 0, 0, 1080, 720);


//    cout << mainComponent->id.size();
    return 0;
}